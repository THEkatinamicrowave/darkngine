name: Mac OS Builds

on:
  push:
  workflow_dispatch:

env:
  HAXE_VERSION: 4.3.7
  HXCPP_COMPILE_CACHE: ${{ github.workspace }}/.hxcpp_cache

jobs:
  intel_build:
    name: Mac OS x64 Build
    permissions: write-all
    runs-on: macos-26
    steps:
      - name: Pulling the new commit
        uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - name: Setting up Haxe
        uses: krdlab/setup-haxe@v2
        with:
          haxe-version: ${{ env.HAXE_VERSION }}
      # - name: Restore existing build cache for faster compilation
      #   uses: actions/cache@v6
      #   with:
      #     # not caching the bin folder to prevent asset duplication and stuff like that
      #     key: cache-build-mac-intel
      #     path: |
      #       .haxelib/
      #       export/release/macos/obj/
      - name: Installing/Updating libraries
        run: |
          haxelib --global update haxelib
          haxe -cp commandline -D analyzer-optimize --run Main setup -si
      - name: Building the game
        run: |
          haxelib run lime setup -alias -y -nocffi
          haxelib run lime rebuild hxcpp
          haxelib run lime rebuild tools
          arch -x86_64 haxelib run lime rebuild mac -nocolor -release
          arch -x86_64 haxelib run lime build mac -DCOMPILE_EXPERIMENTAL
      - name: Tar files
        run: tar -zcvf DarkNgine.tar.gz -C export/release/macos/bin .
      - name: Uploading artifact (entire build)
        uses: actions/upload-artifact@v7
        with:
          name: DarkNgine-x64
          path: DarkNgine.tar.gz
      # - name: Clearing already existing cache
      #   uses: actions/github-script@v9
      #   with:
      #     script: |
      #       const caches = await github.rest.actions.getActionsCacheList({
      #         owner: context.repo.owner,
      #         repo: context.repo.repo,
      #       })
      #       for (const cache of caches.data.actions_caches) {
      #         if (cache.key == "cache-build-mac-intel") {
      #           console.log('Clearing ' + cache.key + '...')
      #           await github.rest.actions.deleteActionsCacheById({
      #             owner: context.repo.owner,
      #             repo: context.repo.repo,
      #             cache_id: cache.id,
      #           })
      #           console.log("Cache cleared.")
      #         }
      #       }
      # - name: Uploading new cache
      #   uses: actions/cache@v6
      #   with:
      #     # caching again since for some reason it doesnt work with the first post cache shit
      #     key: cache-build-mac-intel
      #     path: |
      #       .haxelib/
      #       export/release/macos/obj/

  arm_build:
    name: Mac OS ARM64 Build
    permissions: write-all
    runs-on: macos-26
    steps:
      - name: Pulling the new commit
        uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - name: Setting up Haxe
        uses: krdlab/setup-haxe@v2
        with:
          haxe-version: ${{ env.HAXE_VERSION }}
      # - name: Restore existing build cache for faster compilation
      #   uses: actions/cache@v6
      #   with:
      #     # not caching the bin folder to prevent asset duplication and stuff like that
      #     key: cache-build-mac-arm
      #     path: |
      #       .haxelib/
      #       export/release/macos/obj/
      - name: Installing/Updating libraries
        run: |
          haxelib --global update haxelib
          haxe -cp commandline -D analyzer-optimize --run Main setup -si
      - name: Building the game
        run: |
          haxelib run lime setup -alias -y -nocffi
          haxelib run lime rebuild hxcpp
          haxelib run lime rebuild tools
          haxelib run lime rebuild mac -nocolor -release
          haxelib run lime build mac -DCOMPILE_EXPERIMENTAL
      - name: Tar files
        run: tar -zcvf DarkNgine.tar.gz -C export/release/macos/bin .
      - name: Uploading artifact (entire build)
        uses: actions/upload-artifact@v7
        with:
          name: DarkNgine-ARM64
          path: DarkNgine.tar.gz
      # - name: Clearing already existing cache
      #   uses: actions/github-script@v9
      #   with:
      #     script: |
      #       const caches = await github.rest.actions.getActionsCacheList({
      #         owner: context.repo.owner,
      #         repo: context.repo.repo,
      #       })
      #       for (const cache of caches.data.actions_caches) {
      #         if (cache.key == "cache-build-mac-arm") {
      #           console.log('Clearing ' + cache.key + '...')
      #           await github.rest.actions.deleteActionsCacheById({
      #             owner: context.repo.owner,
      #             repo: context.repo.repo,
      #             cache_id: cache.id,
      #           })
      #           console.log("Cache cleared.")
      #         }
      #       }
      # - name: Uploading new cache
      #   uses: actions/cache@v6
      #   with:
      #     # caching again since for some reason it doesnt work with the first post cache shit
      #     key: cache-build-mac-arm
      #     path: |
      #       .haxelib/
      #       export/release/macos/obj/

  universal_build:
    name: Mac OS Universal Build
    permissions: write-all
    runs-on: macos-26
    needs: 
      - intel_build
      - arm_build
    steps:
      - name: Download Mac OS x64 Full Build
        uses: actions/download-artifact@v8
        with:
          name: DarkNgine-x64
          path: intel
      - name: Download Mac OS ARM64 Full Build
        uses: actions/download-artifact@v8
        with:
          name: DarkNgine-ARM64
          path: arm
      - name: Tar files (x64)
        run: tar -xvf intel/DarkNgine.tar.gz -C intel
      - name: Tar files (ARM64)
        run: tar -xvf arm/DarkNgine.tar.gz -C arm
      - name: Removing ARM64 and x64 artifacts
        uses: geekyeggo/delete-artifact@v6
        with:
          name: DarkNgine-*
      - name: Creating Universal App Bundle
        shell: bash
        run: |
          BIN="DarkNgine.app/Contents/MacOS"
          INTEL="intel/DarkNgine.app/Contents/MacOS"
          ARM="arm/DarkNgine.app/Contents/MacOS"
          cp -R intel/DarkNgine.app DarkNgine.app
          lipo -create "$INTEL/DarkNgine" "$ARM/DarkNgine" -output "$BIN/DarkNgine"
          lipo -create "$INTEL/lime.ndll" "$ARM/lime.ndll" -output "$BIN/lime.ndll"
      - name: Uploading Universal Mac OS bundle (executable)
        uses: actions/upload-artifact@v7
        with:
          name: DarkNgine (Executable Only)
          path: DarkNgine.app/Contents/MacOS/DarkNgine
      - name: Uploading Universal Mac OS bundle
        uses: actions/upload-artifact@v7
        with:
          name: DarkNgine
          path: DarkNgine.app

# didnt compile debug in the same job or github would have said that job wasn't completed until debug was done too (debug uploads are not essential)
  intel_debug_build:
    name: Mac OS x64 Debug Build
    permissions: write-all
    runs-on: macos-26
    needs: universal_build # since its low priority, it'll run after, so actions will concentrate first on normal builds
    steps:
      - name: Pulling the new commit
        uses: actions/checkout@v7
      - name: Setting up Haxe
        uses: krdlab/setup-haxe@v2
        with:
          haxe-version: ${{ env.HAXE_VERSION }}
      # - name: Restore existing build cache for faster compilation
      #   uses: actions/cache@v6
      #   with:
      #     # not caching the bin folder to prevent asset duplication and stuff like that
      #     key: cache-build-mac-intel-debug
      #     path: |
      #       .haxelib/
      #       export/debug/macos/obj/
      - name: Installing/Updating libraries
        run: |
          haxelib --global update haxelib
          haxe -cp commandline -D analyzer-optimize --run Main setup -si
      - name: Building the game
        run: |
          haxelib run lime setup -alias -y -nocffi
          haxelib run lime rebuild hxcpp
          haxelib run lime rebuild tools
          arch -x86_64 haxelib run lime rebuild mac -nocolor -release
          arch -x86_64 haxelib run lime build mac -debug -DCOMPILE_EXPERIMENTAL
      - name: Tar files
        run: tar -zcvf DarkNgine.tar.gz -C export/debug/macos/bin .
      - name: Uploading artifact (entire build)
        uses: actions/upload-artifact@v7
        with:
          name: DarkNgine-x64 Debug
          path: DarkNgine.tar.gz
      # - name: Clearing already existing cache
      #   uses: actions/github-script@v9
      #   with:
      #     script: |
      #       const caches = await github.rest.actions.getActionsCacheList({
      #         owner: context.repo.owner,
      #         repo: context.repo.repo,
      #       })
      #       for (const cache of caches.data.actions_caches) {
      #         if (cache.key == "cache-build-mac-intel-debug") {
      #           console.log('Clearing ' + cache.key + '...')
      #           await github.rest.actions.deleteActionsCacheById({
      #             owner: context.repo.owner,
      #             repo: context.repo.repo,
      #             cache_id: cache.id,
      #           })
      #           console.log("Cache cleared.")
      #         }
      #       }
      # - name: Uploading new cache
      #   uses: actions/cache@v6
      #   with:
      #     # caching again since for some reason it doesnt work with the first post cache shit
      #     key: cache-build-mac-intel-debug
      #     path: |
      #       .haxelib/
      #       export/debug/macos/obj/

  arm_debug_build:
    name: Mac OS ARM64 Debug Build
    permissions: write-all
    runs-on: macos-26
    needs: universal_build # since its low priority, it'll run after, so actions will concentrate first on normal builds
    steps:
      - name: Pulling the new commit
        uses: actions/checkout@v7
      - name: Setting up Haxe
        uses: krdlab/setup-haxe@v2
        with:
          haxe-version: ${{ env.HAXE_VERSION }}
      # - name: Restore existing build cache for faster compilation
      #   uses: actions/cache@v6
      #   with:
      #     # not caching the bin folder to prevent asset duplication and stuff like that
      #     key: cache-build-mac-arm-debug
      #     path: |
      #       .haxelib/
      #       export/debug/macos/obj/
      - name: Installing/Updating libraries
        run: |
          haxelib --global update haxelib
          haxe -cp commandline -D analyzer-optimize --run Main setup -si
      - name: Building the game
        run: |
          haxelib run lime setup -alias -y -nocffi
          haxelib run lime rebuild hxcpp
          haxelib run lime rebuild tools
          haxelib run lime rebuild mac -nocolor -release
          haxelib run lime build mac -debug -DCOMPILE_EXPERIMENTAL
      - name: Tar files
        run: tar -zcvf DarkNgine.tar.gz -C export/debug/macos/bin .
      - name: Uploading artifact (entire build)
        uses: actions/upload-artifact@v7
        with:
          name: DarkNgine-ARM64 Debug
          path: DarkNgine.tar.gz
      # - name: Clearing already existing cache
      #   uses: actions/github-script@v9
      #   with:
      #     script: |
      #       const caches = await github.rest.actions.getActionsCacheList({
      #         owner: context.repo.owner,
      #         repo: context.repo.repo,
      #       })
      #       for (const cache of caches.data.actions_caches) {
      #         if (cache.key == "cache-build-mac-arm-debug") {
      #           console.log('Clearing ' + cache.key + '...')
      #           await github.rest.actions.deleteActionsCacheById({
      #             owner: context.repo.owner,
      #             repo: context.repo.repo,
      #             cache_id: cache.id,
      #           })
      #           console.log("Cache cleared.")
      #         }
      #       }
      # - name: Uploading new cache
      #   uses: actions/cache@v6
      #   with:
      #     # caching again since for some reason it doesnt work with the first post cache shit
      #     key: cache-build-mac-arm-debug
      #     path: |
      #       .haxelib/
      #       export/debug/macos/obj/

  universal_debug_build:
    name: Mac OS Universal Debug Build
    permissions: write-all
    runs-on: macos-26
    needs: 
      - intel_debug_build
      - arm_debug_build
    steps:
      - name: Download Mac OS x64 Full Build
        uses: actions/download-artifact@v8
        with:
          name: DarkNgine-x64 Debug
          path: intel
      - name: Download Mac OS ARM64 Full Build
        uses: actions/download-artifact@v8
        with:
          name: DarkNgine-ARM64 Debug
          path: arm
      - name: Tar files (x64)
        run: tar -xvf intel/DarkNgine.tar.gz -C intel
      - name: Tar files (ARM64)
        run: tar -xvf arm/DarkNgine.tar.gz -C arm
      - name: Removing ARM64 and x64 artifacts
        uses: geekyeggo/delete-artifact@v6
        with:
          name: DarkNgine-*
      - name: Creating Universal App Bundle
        shell: bash
        run: |
          BIN="DarkNgine.app/Contents/MacOS"
          INTEL="intel/DarkNgine.app/Contents/MacOS"
          ARM="arm/DarkNgine.app/Contents/MacOS"
          cp -R intel/DarkNgine.app DarkNgine.app
          lipo -create "$INTEL/DarkNgine" "$ARM/DarkNgine" -output "$BIN/DarkNgine"
          lipo -create "$INTEL/lime.ndll" "$ARM/lime.ndll" -output "$BIN/lime.ndll"
      - name: Uploading Universal Mac OS bundle
        uses: actions/upload-artifact@v7
        with:
          name: DarkNgine Debug
          path: DarkNgine.app
